{
  "name": "gemini-text-learn",
  "version": "1.0.0",
  "private": true,
  "description": "A ChatGPT/Gemini-style chat web app for conversation and learning, backed by an OpenAI-compatible gateway.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c green,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
